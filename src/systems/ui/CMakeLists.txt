set (
  SOURCES
  ${SOURCES}
  "${CMAKE_CURRENT_LIST_DIR}/mod.c"
  "${CMAKE_CURRENT_LIST_DIR}/widget_runesel.c"
  "${CMAKE_CURRENT_LIST_DIR}/widget_colsel.c"
  "${CMAKE_CURRENT_LIST_DIR}/widget_tilesel.c"
  PARENT_SCOPE
)
