set (
  SOURCES
  ${SOURCES}
  "${CMAKE_CURRENT_LIST_DIR}/u_main.c"
  "${CMAKE_CURRENT_LIST_DIR}/u_window.c"
  "${CMAKE_CURRENT_LIST_DIR}/u_widget_rune_selector.c"
  "${CMAKE_CURRENT_LIST_DIR}/u_dialog_rune_selector.c"
  PARENT_SCOPE
)
