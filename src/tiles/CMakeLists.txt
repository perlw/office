set (
  SOURCES
  ${SOURCES}
  "${CMAKE_CURRENT_LIST_DIR}/t_main.c"
  "${CMAKE_CURRENT_LIST_DIR}/t_ascii.c"
  "${CMAKE_CURRENT_LIST_DIR}/t_surface.c"
  "${CMAKE_CURRENT_LIST_DIR}/t_tileset.c"
  PARENT_SCOPE
)
